# NOTE: Replace placeholder values with base64 encoded secrets
# Generate with: echo -n "your-value" | base64
#
# Or create via kubectl:
# kubectl create secret generic todo-secrets \
#   --from-literal=database-url="$DATABASE_URL" \
#   --from-literal=cohere-api-key="$COHERE_API_KEY" \
#   --from-literal=jwt-secret="$JWT_SECRET" \
#   -n todo-app
#
apiVersion: v1
kind: Secret
metadata:
  name: todo-secrets
  namespace: todo-app
type: Opaque
data:
  # Base64 encoded values - REPLACE THESE
  database-url: cGxhY2Vob2xkZXI=
  cohere-api-key: cGxhY2Vob2xkZXI=
  jwt-secret: cGxhY2Vob2xkZXI=
